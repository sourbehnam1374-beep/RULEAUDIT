# SHIVA SPIN-THA v7.4 - archived run index

This path is retained for compatibility with the pre-v0.2.1 repository. The
earlier text snapshot used unseeded Sobol confidence estimates and legacy
mechanistic labels, so it is superseded.

The current seeded report, tables, and figures are versioned at
[`examples/shiva/out/report.md`](../examples/shiva/out/report.md). Reproduce
them with `python examples/shiva/run.py`.

This encoding was reconstructed from internal audit documentation as an
illustrative software example. It is not an independently validated reference
implementation, publication record, or source of clinical-validity evidence.
